
<!--pages/paidui/paidui.wxml-->

  <view bindtap="bindViewTap" class="userinfo">
    <image class="userinfo-avatar" src="{{userInfo.avatarUrl}}" background-size="cover"></image>
   <text class="user-motto">{{paidui}}</text>
    <text class="userinfo-nickname">{{userInfo.nickName}}</text>
  </view>
  <view class="button-wrapper2">
    <text>排队成功</text>
    <button type="primary" size="{{warnSize}}" loading="{{loading}}" plain="{{plain}}" disabled="{{disabled}}" bindtap="return"> 返回首页
    </button>
    </view>
<!--<modal hidden="{{hidden}}" title="{{bank}}" confirm-text="确定" cancel-text="取消" bindcancel="cancel" bindconfirm="confirm" no-cancel="{{nocancel}}">
  你已经排队成功,排在你前面还有30人,预计15分钟后将为办理业务,请提前前往该网点
</modal>-->